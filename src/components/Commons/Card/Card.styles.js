import styled from 'styled-components';

export const BlockPeopleTaskItem = styled.li`

	max-width: 280px;
	
	position: relative;
	list-style: none;
	
	&:not(:last-child){
		margin-right:20px;
		margin-bottom: 20px;
	}
`;
